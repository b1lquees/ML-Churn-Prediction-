[pytest]
testpaths = tests
python_files = test_*.py
addopts = -q --strict-markers
filterwarnings =
    # Third-party deprecation noise (mlflow 2.14 on pydantic v2, pkg_resources)
    ignore::DeprecationWarning
    ignore::UserWarning
